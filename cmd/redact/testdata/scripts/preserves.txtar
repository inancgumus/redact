# Plain prose passes through unchanged.
exec ./redact -string 'the quick brown fox'
stdout '^the quick brown fox$'
! stdout '\*'

# Variable expansion is preserved.
stdin var.txt
exec ./redact
stdout '\$\{SECRET\}'
! stdout '\*'

-- var.txt --
password="${SECRET}"
